import Frame, {Tabs} from "../components/wallet/frame";

const Send = () => {
    return (
        <Frame selected={Tabs.Send}>
            <h2>Send</h2>
        </Frame>
    )
}

export default Send
