const {GetFollowing, GetFollowers, GetRecentFollow} = require("./memo_follow")
const {GetPosts} = require("./memo_post")
const {GetCoins} = require("./outputs")

module.exports = {
    GetFollowing,
    GetFollowers,
    GetRecentFollow,
    GetPosts,
    GetCoins,
}
